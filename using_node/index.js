// to use npm we MUST have installed NodeJS
// npm is the Node Package Manager
// npx is also NPM but with eXecution capabilities - run immediatly!

// we use 'npm install jquery -save' to download the latest
// jquery and save it's dependency in package.json
// then if we ever need to re-install dependencies, we use
// npm install
